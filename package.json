{
  "name": "social-look-monorepo",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "Monorepo containing Character App and Admin Panel",
  "workspaces": [
    "character-app",
    "admin-app"
  ],
  "scripts": {
    "dev:character": "cd character-app && npm run dev",
    "dev:admin": "cd admin-app && npm run dev",
    "dev:api": "npm --prefix cloudflare/pika-api run dev",
    "build:character": "cd character-app && npm run build",
    "build:admin": "cd admin-app && npm run build",
    "check:api": "npm --prefix cloudflare/pika-api run check",
    "deploy:api": "npm --prefix cloudflare/pika-api run deploy"
  },
  "dependencies": {},
  "devDependencies": {}
}
